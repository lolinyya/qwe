<form method="post" action="{{ path('app_dishes_delete', {'id': dish.id}) }}" onsubmit="return confirm('Удалить?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dish.id) }}">
    <button class="btn">Удалить</button>
</form>
