<form method="post" action="{{ path('app_people_delete', {'id': person.id}) }}" onsubmit="return confirm('Удалить?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ person.id) }}">
    <button class="btn">Удалить</button>
</form>
